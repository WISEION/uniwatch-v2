# operations/

Deploy/runbook artifacts (production authorization, on-call, rollback). Depends on `D-HOST`/`D-IDP` (owner decisions, see `docs/CONTEXT.md`) for anything beyond local/dev. Empty in Phase 0/1.
