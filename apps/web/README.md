# apps/web

React/TypeScript UI (`NFR-ARC-01`). Command Center, tender detail, deep links, Algorithm builder — Phase 2+ scope per `docs/reports/PLAN-MISSION-1.md`. All permission checks shown here are re-verified server-side; this app never is the source of authorization truth (`FR-ADM-02`).

Not implemented yet — out of Mission 1 (Phase 0/1) scope entirely.
