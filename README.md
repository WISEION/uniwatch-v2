# UNIWatch-v2

Tender / Vendor / Decision Intelligence for Unico QSC. Modular monolith: React/TypeScript web, Python/FastAPI API, separate Python worker, PostgreSQL.

**Before touching this repo, read [`AGENTS.md`](AGENTS.md) and [`docs/CONTEXT.md`](docs/CONTEXT.md).** This is a from-scratch rebuild of `Tendet Watcher` / `UNIWatch` v1 — v1 paths are off-limits (see `NEG-01`, `NEG-02`, `tools/check-v1-untouched.*`).

Status: Phase 0 (Foundation), task 0.A (architect) in progress. No application code yet — see `docs/reports/PLAN-MISSION-1.md` and `docs/reports/WORKLOG.md`.
