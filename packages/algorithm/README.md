# packages/algorithm

Versionable policy-graph builder: `Human` / `Rule` / `Gate` node types. ML is advisory-only and blocked until Phase 8 (owner-locked decision). Owns policy graphs, evaluations, and approvals — never business facts (`docs/adr/0001-modular-monolith-boundaries.md`).

Not implemented yet — out of Mission 1 (Phase 0/1) scope entirely.
