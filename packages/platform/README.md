# packages/platform

Cross-cutting concerns: auth, RBAC (deny-by-default, `FR-ADM-01..05`), audit, observability, migrations glue, egress validator / trusted source registry (`NFR-SEC-01..03`). No domain scoring or business-decision logic lives here (`docs/adr/0001-modular-monolith-boundaries.md`).

Not implemented yet — 0.B/0.C scope.
