# packages/decision

Bid/No-Bid workflow, No-Go hard stop, outcomes (`FR-DEC-*`, `INV-06`). Stores references to immutable input versions, never a mutable copy of another domain's "current" state (`docs/adr/0001-modular-monolith-boundaries.md`, `docs/adr/0003-data-authority-and-provenance.md`).

Not implemented yet — out of Mission 1 (Phase 0/1) scope; domain invariants only (see `docs/reports/PLAN-MISSION-1.md` §5 on P005).
